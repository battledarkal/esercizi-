#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#define DIM 18

int main(){

srand(time(NULL));

	int array[DIM], i, j, conta;

	for(i = 0; i < DIM; i++) {
        do {
            array[i] = rand() % 90 + 1;
            conta = 0;
            for(j = 0; j < i; j++) {
                if(array[i] == array[j]) {
                    conta = 1;
                }
            }
        } while(uguale == 1);
        
        printf("%d\t", array[i]);
    }
    
	return 0;
}
